#ifndef ROTATION_H
#define ROTATION_H

#include "spawn_block.h"

void RotateTetrominoClockwise(TetrominoType t,
                             uint16_t *x, uint16_t *y, uint8_t *rot);

#endif /* ROTATION_H */